apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'eclipse'

archivesBaseName = "RepairTool"
version = "1.0.1-SNAPSHOT"
libsDirName = "../../libs/${version}"

sourceSets {
    main {
        java {
            srcDir "src"
        }
    }
}
